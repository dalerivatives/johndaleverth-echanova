# v72 — Clean section URLs + protected human backdrop

- Added real portfolio routes: `/projects`, `/achievements`, `/tools`, and `/chat`.
- Sidebar navigation now updates the browser address bar with History API and supports Back/Forward.
- Direct refreshes on clean section URLs load the correct SPA shell.
- Legacy `/index.html` now redirects to `/`, so `index.html` does not remain visible in the address bar.
- Added a responsive exclusion mask around the coded/real human so floating code and algorithm trees cannot visually overlap the person.
- Kept the background effects everywhere else.
